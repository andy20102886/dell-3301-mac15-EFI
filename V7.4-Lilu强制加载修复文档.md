# V7.4 - Lilu 强制加载修复技术文档

## 修复时间
2026-06-04 23:38

## 当前状态（V7.3 重启后）
- GPU：仍显示 7MB VRAM，`Kernel Extension Info: No Kext Loaded`
- Lilu 1.7.2 + WEG 1.7.1 + AMFIPass 1.4.1 均已放入 OC/Kexts/
- config.plist 中三个 kext 均 Enabled=true
- boot-args 已设置：`debug=0x100 keepsyms=1 amfi=0x80 -lilubetaall -wegbeta`
- csr-active-config=FF0F0000，SecureBootModel=Disabled
- **问题：Lilu 仍然没有加载（kextstat/kmutil showloaded 均无输出）**

## 根本原因分析
Lilu 1.7.2 虽然在理论上支持 macOS 15（Darwin 24），但在某些配置下仍会拒绝加载。
`-lilubetaall` 参数允许 beta 版 Lilu 加载，但可能不足以强制加载。

**新增参数 `-liluforce`**：
- 强制 Lilu 在所有 macOS 版本上加载，绕过 Darwin 版本检测
- 这是解决 "No Kext Loaded" 的最后手段

## V7.4 修改内容

### 1. boot-args 增加 -liluforce
```diff
- debug=0x100 keepsyms=1 amfi=0x80 -lilubetaall -wegbeta
+ debug=0x100 keepsyms=1 amfi=0x80 -lilubetaall -wegbeta -liluforce
```

### 2. 备份文件
- `/Volumes/OC/EFI/OC/config.plist.bak.v7.4.20260604234126`

## 文件变更清单
| 文件 | 操作 | 说明 |
|------|------|------|
| config.plist | 修改 | NVRAM:Add boot-args 加 -liluforce |
| config.plist.bak.v7.4.* | 新增 | 备份 |

## 下次启动验证步骤
1. 开机时按 `Esc` 进入 OpenCore Boot Picker
2. 按 `空格键` 显示辅助选项
3. 选择 `Reset NVRAM`，回车确认
4. 自动重启后再次进入 OC Boot Picker
5. 选择 `macOS Installer` / `macOS Sequoia` 正常启动
6. 启动后打开 `系统信息 → 图形卡/显示器`
7. 验证：
   - `VRAM (Total)` 是否从 7MB 变为正常值（如 1536MB）
   - `Kernel Extension Info` 是否显示 `Loaded`
   - 显示器是否显示正确名称和分辨率

## 预期结果
- `-liluforce` 强制 Lilu 加载 → WEG 跟着加载 → Intel UHD 620 加速启用
- VRAM 显示正常（>7MB）
- 系统动画流畅，不再卡顿

## 如果仍然 7MB VRAM
可能原因：
1. AMFIPass.kext 没有正确签名/损坏 → 重新下载 AMFIPass 1.4.1
2. macOS 15 已完全移除 KBL 驱动，OCLP 补丁是唯一方案
3. SMBIOS MacBookPro15,2 不支持 KBL 补丁 → 改用 MacBookPro14,1

## 触摸板/键盘问题
当前 VoodooI2C/VoodooPS2 加载顺序：
- VoodooI2C 插件（VoodooInput/VoodooGPIO）→ 先于主体加载（可能有问题）
- VoodooI2C.kext 主体
- VoodooI2CHID.kext
- VoodooPS2Controller.kext（键盘/PS2 触摸板）

如果触摸板仍时好时坏，需要调整 VoodooI2C 插件加载顺序。

---
*此文档由 WorkBuddy AI 自动生成，供技术参考。*
