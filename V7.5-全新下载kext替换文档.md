# V7.5 — 全新下载 kext 替换（从 GitHub 官方 Release）

## 修改时间
2026-06-04 23:53

## 修改原因
此前 Lilu、WhateverGreen 均未加载（kextstat / kmutil showloaded 无输出），怀疑从 Windows 拷贝的 kext 文件损坏或带 macOS 隔离属性（@ extended attributes）。V7.4 的 `-liluforce` 也没起作用。

决定：从 GitHub 官方 release 重新下载干净版本，彻底排除文件损坏的可能。

## 具体修改

### 1. 备份旧版 kext
```bash
备份目录: /Volumes/OC/EFI/OC/Kexts/old_backup_20260604/
├── Lilu.kext          (旧版 1.7.2, 从 Windows 拷贝)
└── WhateverGreen.kext (旧版 1.7.1, 从 Windows 拷贝)
```

### 2. 从 GitHub 官方 Release 下载新版 kext

| kext | 来源 | 版本 | 下载方式 |
|------|------|------|----------|
| **Lilu.kext** | github.com/acidanthera/Lilu/releases/latest | **1.7.2** | API → curl 直接下载 RELEASE.zip |
| **WhateverGreen.kext** | github.com/acidanthera/WhateverGreen/releases/latest | **1.7.0** | API → curl 直接下载 RELEASE.zip |
| **AMFIPass.kext** | 保留原有 | 1.4.1 | 未替换（amfi=0x80 已禁用 AMFI，AMFIPass 非必须） |

下载到: `/Users/andy/Desktop/kexts_download/`
→ 解压后复制到 `/Volumes/OC/EFI/OC/Kexts/`

### 3. 核实 kext 可执行文件已就位
```
/Volumes/OC/EFI/OC/Kexts/Lilu.kext/Contents/MacOS/Lilu           (527KB)
/Volumes/OC/EFI/OC/Kexts/WhateverGreen.kext/Contents/MacOS/WhateverGreen  (532KB)
```

## 当前 boot-args（继承自 V7.4，未改动）
```
debug=0x100 keepsyms=1 amfi=0x80 -lilubetaall -wegbeta -liluforce
```

## config.plist 备份
- V7.4 备份: `config.plist.bak.v7.4.20260604234126`
- V7.5 未改 config.plist，仅替换 kext 文件

## 验证步骤
1. 重启 → OC Picker → 按空格 → **Reset NVRAM**
2. 自动重启 → 选 macOS 启动
3. 运行: `system_profiler SPDisplaysDataType | head -15`
4. 检查 VRAM 是否不再是 7MB
5. 运行: `kextstat | grep -i -E "lilu|whatever"` 确认 kext 已加载

## 与之前版本的差异

| 版本 | 修改内容 | 结果 |
|------|----------|------|
| V7.2 | 升级 Lilu→1.7.2, WEG→1.7.1, 加 AMFIPass | GPU 仍 7MB |
| V7.3 | 删除错误的连接器 alldata 属性 | GPU 仍 7MB |
| V7.4 | boot-args 加 `-liluforce` | GPU 仍 7MB |
| **V7.5** | **从 GitHub 重新下载 Lilu + WEG，替换旧文件** | **待验证** |

## 如果仍然 7MB
确认 macOS 15 Sequoia 已完全移除 Kaby Lake 显卡驱动框架（AppleIntelKBLGraphics*），单靠 WhateverGreen 注入 platform-id 无法恢复加速。必须走 OCLP 路线：
1. 在 OCLP GUI 中: Settings → Verbose 开启
2. 菜单: Post-Install Root Patch → Force 模式
3. 或命令行: `/Applications/OpenCore-Patcher.app/Contents/MacOS/OpenCore-Patcher --patch_sys_vol`
