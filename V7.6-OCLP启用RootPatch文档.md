# V7.6 — 启用 OCLP Root Patch（修改 csr-active-config）

## 修改时间
2026-06-05 00:06

## 问题诊断
V7.5 确认：即使从 GitHub 重新下载 Lilu + WEG，kext 仍然零加载。WhateverGreen 注入路线完全失败。

**根因确认**: macOS 15 Sequoia 已完全移除 `AppleIntelKBLGraphics` 内核扩展框架（ioreg 中 0 个 KBL framebuffer）。单靠 WhateverGreen 注入 platform-id 永远无法恢复 GPU 加速。

**唯一剩余方案**: OCLP Root Patch — 从旧版 macOS 提取 KBL 显卡驱动文件打入当前系统。

### 为什么 OCLP Root Patch 按钮是灰色的
OCLP 2.4.1 已安装，但运行时 `payloads.dmg` 挂载失败：
```
hdiutil: attach failed - 操作不被允许
```
**原因**: `csr-active-config=FF0F0000` 没有完全关闭 SIP 的文件系统保护（`CSR_ALLOW_UNRESTRICTED_FS` 位未设）。

## 修改内容

| # | 参数 | 位置 | 改前 | 改后 | 原因 |
|---|------|------|------|------|------|
| 1 | csr-active-config | NVRAM:Add | `FF0F0000` | `FF070000` | 完全关闭 SIP（含文件系统保护） |

### csr-active-config 各值含义

| 值 | SIP 状态 | 文件系统保护 | OCLP 兼容 |
|----|----------|-------------|-----------|
| `00000000` | 完全开启 | ✅ | ❌ |
| `FF0F0000` | 部分关闭（自定义） | ❌ 未完全关 | ❌ |
| **`FF070000`** | **完全关闭** | **✅ 关闭** | **✅** |

## config.plist 备份
- `config.plist.bak.v7.6.20260605000657`

## boot-args（保持 V7.5 不变）
```
debug=0x100 keepsyms=1 amfi=0x80 -lilubetaall -wegbeta -liluforce
```

## 重启后操作步骤

### Step 1: 验证 SIP 已关闭
```bash
csrutil status
# 应输出: System Integrity Protection status: disabled.
```

### Step 2: 运行 OCLP 打补丁
1. 打开 `/Applications/OpenCore-Patcher.app`
2. 点击 **Post-Install Root Patch**
3. **Start Root Patching** 按钮应该不再灰色
4. 点击开始，等待完成
5. **重启**

### Step 3: 验证 GPU 加速
```bash
system_profiler SPDisplaysDataType | grep VRAM
# 正常: VRAM (Total): 1536 MB（不再是 7MB）

kextstat | grep -i "IntelHD\|AppleIntel"
# 正常: 应有 AppleIntelKBLGraphics 等 kext 出现
```

### 如果 OCLP GUI 仍然灰色
用命令行强制模式：
```bash
cd /Applications/OpenCore-Patcher.app/Contents/MacOS
./OpenCore-Patcher --patch_sys_vol
```

## 回退方案
恢复 V7.5 备份：
```bash
cp /Volumes/OC/EFI/OC/config.plist.bak.v7.5 /Volumes/OC/EFI/OC/config.plist
```
