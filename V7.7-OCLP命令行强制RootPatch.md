# V7.7 — OCLP 命令行强制 Root Patch

## 修改时间
2026-06-05 00:18

## 问题分析

### OCLP GUI 按钮灰色原因
- 当前 SMBIOS: **MacBookPro15,2**（Coffee Lake 2018）
- OCLP 检测到 MacBookPro15,2 原生支持 GPU → 不需要补丁 → 按钮灰色
- 实际硬件是 Kaby Lake-R UHD 620，macOS 15 不为 CFL SMBIOS 加载 KBL 驱动

### 不能改 SMBIOS 到 Kaby Lake 机型
macOS 15 Sequoia 的 PlatformSupport.plist **完全没有 Kaby Lake 机型**（最老是 2018 Coffee Lake）。改回 MacBookPro14,1 会：
- 卡 `OCB: Failed to match a default boot option`
- 或出现禁止符号 🚫
- **这就是 V4 失败的原因**

## V7.7 方案：OCLP 命令行强制打补丁

不通过 GUI，直接用 `--patch_sys_vol` 命令行强制 OCLP 打 KBL 显卡驱动补丁。

### 命令
```bash
/Applications/OpenCore-Patcher.app/Contents/MacOS/OpenCore-Patcher --patch_sys_vol
```

### 原理
`--patch_sys_vol` 强制 OCLP 跳过机型检测，直接从 payloads.dmg 提取 KBL 显卡驱动文件打入 `/System/Library/Extensions/`。

### 如果命令失败

#### 故障 A: "hdiutil: attach failed - 操作不被允许"
- 原因: SIP 文件系统保护未完全关闭
- 修复: 确认 `csr-active-config = FF070000`（当前已是 FF070000 ✅）
- 验证: `csrutil status | grep "Filesystem Protections"` 应显示 `disabled`

#### 故障 B: "No matching patches found for this model"
- 原因: OCLP 命令行也做了机型检测
- 修复: 用 `--model` 参数指定一个 Kaby Lake 机型
```bash
/Applications/OpenCore-Patcher.app/Contents/MacOS/OpenCore-Patcher --patch_sys_vol --model MacBookPro14,1
```

#### 故障 C: 打完补丁重启后卡进度条
- 原因: 打入的 KBL 驱动和当前 board-id 不匹配，显卡驱动 panic
- **紧急修复（不用重新安装系统）:**

**方法 1 — 从 OC 启动时临时禁用 GPU 加速:**
1. 开机进 OC Picker
2. 按空格 → 选中 macOS → 不要直接回车
3. 输入以下 boot-arg 追加: `-igfxvesa`
4. 回车启动（VESA 模式，无加速但能亮屏）
5. 进系统后运行 OCLP 撤销补丁:
```bash
/Applications/OpenCore-Patcher.app/Contents/MacOS/OpenCore-Patcher --unpatch_sys_vol
```
6. 重启

**方法 2 — 从 Recovery 恢复:**
1. 重启 → OC Picker → 选择 Recovery 分区
2. 终端运行:
```bash
# 挂载系统卷
mount -uw /Volumes/Macintosh\ HD
# 删除 OCLP 打的补丁 kext
rm -rf /Volumes/Macintosh\ HD/System/Library/Extensions/AppleIntelKBLGraphics*.kext
# 重建 kext 缓存
kmutil install --volume-root /Volumes/Macintosh\ HD --update-all
bless --mount /Volumes/Macintosh\ HD --bootefi --create-snapshot
```
3. 重启

**方法 3 — 从 OC config 回退（最安全）:**
1. 在 Windows 或另一个 macOS 下挂载 EFI 分区
2. 恢复 config.plist 备份:
```bash
cp /Volumes/OC/EFI/OC/config.plist.bak.v7.6.20260605000657 /Volumes/OC/EFI/OC/config.plist
```
3. 重启 → Reset NVRAM → 正常启动

#### 故障 D: 打完补丁后系统启动但 GPU 还是 7MB
- 原因: OCLP 打的驱动不匹配当前 boot-args/kext 配置
- 修复: 重启 → Reset NVRAM → 看效果（可能需要去掉 `-lilubetaall -wegbeta -liluforce`）

### 成功标志
```bash
system_profiler SPDisplaysDataType | grep VRAM
# 正常: VRAM (Total): 1536 MB

kextstat | grep -i KBL
# 正常: 应有 AppleIntelKBLGraphics 等 kext 出现

ioreg -l -w0 | grep -c "AppleIntelKBLGraphics"
# 正常: ≥ 1
```

## config.plist 备份
- 本次未修改 config.plist
- 上一个有效备份: `config.plist.bak.v7.6.20260605000657`

## 回退方案优先级
1. OC boot-arg 加 `-igfxvesa` → 亮屏 → `--unpatch_sys_vol`
2. Recovery 手动删除 KBL kext
3. Windows 侧恢复 config 备份
