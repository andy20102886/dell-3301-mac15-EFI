# V7.8 - OpenCore 强制注入 KBL 驱动（绕过 OCLP）

## 修改时间
2026-06-05 00:23

## 之前状态
- OCLP `--patch_sys_vol` 拒绝打补丁：`No Root Patches required for your machine!`
- 原因：SMBIOS = `MacBookPro15,2`（Coffee Lake），OCLP 认为原生支持，无需补丁
- macOS 15 的 `kmutil` 主动排除 KBL kext，S/L/E 里有但永不加载

## V7.8 修改内容

### 1. 复制 KBL kext 到 OC 盘
| 源路径 | 目标路径 |
|---------|---------|
| `/S/L/E/AppleIntelKBLGraphics.kext` | `/Volumes/OC/EFI/OC/Kexts/KBL_drivers/` |
| `/S/L/E/AppleIntelKBLGraphicsFramebuffer.kext` | `/Volumes/OC/EFI/OC/Kexts/KBL_drivers/` |

### 2. 修改 config.plist

**备份文件**: `config.plist.bak.v7.8.20260605002303`

**修改区段**: `Kernel → Add`（索引 1-2 插入）

| # | 参数 | 值 |
|---|------|-----|
| 1 | BundlePath | `Kexts/KBL_drivers/AppleIntelKBLGraphics.kext` |
| 2 | ExecutablePath | `Contents/MacOS/AppleIntelKBLGraphics` |
| 3 | PlistPath | `Contents/Info.plist` |
| 4 | Enabled | `true` |
| 5 | Comment | `KBL GPU driver (forced inject for macOS 15)` |

| # | 参数 | 值 |
|---|------|-----|
| 1 | BundlePath | `Kexts/KBL_drivers/AppleIntelKBLGraphicsFramebuffer.kext` |
| 2 | ExecutablePath | `Contents/MacOS/AppleIntelKBLGraphicsFramebuffer` |
| 3 | PlistPath | `Contents/Info.plist` |
| 4 | Enabled | `true` |
| 5 | Comment | `KBL Framebuffer (forced inject for macOS 15)` |

**插入位置**: Lilu (index 0) 之后，WEG (原 index 1，现 index 3) 之前

**Kernel/Add 现在共 19 个条目**

## 原理
OpenCore 的 `Kernel/Add` 在 OC 启动阶段把 kext 注入 XNU，早于 macOS 内核排除列表生效。
KBL kext 由 OpenCore 直接加载，绕过 `kmutil` 的排除机制。

## 预期结果
- `kextstat` 能看到 `com.apple.driver.AppleIntelKBLGraphics`
- 系统信息 → 图形卡/显示器 → VRAM 显示 `1536 MB`
- 硬件加速恢复，显示不再卡顿

## 失败回退方案

### 方案 A：恢复 config.plist
1. 启动时按 `Esc` → OC Picker
2. 按空格 → 选 `Reset NVRAM`
3. 进 Windows → 挂载 EFI → 恢复 `config.plist.bak.v7.8.*`
4. 或直接选备份文件重命名为 `config.plist`

### 方案 B：进系统后撤销
如果能进系统（可能依然 7MB 但可用）：
```bash
# 在 config.plist 里把两个 KBL 条目的 Enabled 改为 false
```
或用 OC 启动菜单按 `Space` → `Toggle Kext` 禁用。

## 验证命令
```bash
# 进系统后跑
system_profiler SPDisplaysDataType | head -15
kextstat | grep -i "KBL\|IntelHD\|Accelerator"
ioreg -l | grep "AppleIntelKBL"
```

---
> 📄 关联文档:  
> - `/Volumes/OC/V7.7-OCLP命令行强制RootPatch.md`  
> - `/Volumes/OC/V7-完整修复记录.md`
