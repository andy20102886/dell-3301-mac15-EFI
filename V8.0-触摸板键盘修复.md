# V8.0 — 触摸板和键盘修复技术文档

> 📅 2026-06-05  
> 🖥️ Dell Latitude 3301 / macOS 15.5 Sequoia  
> 🔧 修复版本：V8.0（基于 V7.8 GPU 配置之上）

---

## 一、故障现象

- 内置键盘：时好时坏，随机失效，重启有时好有时坏
- 触摸板：时好时坏，有时完全不响应，重启后有时恢复

---

## 二、诊断过程

### 2.1 检查 kext 加载状态

```bash
ioreg -l -w0 | grep -o '"ApplePS2Controller"=[0-9]*'     # → =1 (已加载)
ioreg -l -w0 | grep -o '"VoodooI2CControllerDriver"=[0-9]*'  # → =2 (已加载)
ioreg -l -w0 | grep -o '"VoodooI2CHIDDevice"=[0-9]*'     # → =0 (未匹配！)
```

### 2.2 发现 PS2 冲突

```
ApplePS2Controller → ApplePS2KeyboardDevice → ApplePS2Keyboard (系统驱动，已加载)
VoodooPS2Controller (OC 注入，也在加载) ← 与上面冲突
```

**结论**：系统内置 `ApplePS2Controller` 和 OC 注入的 `VoodooPS2Controller` 同时争抢 PS2 键盘控制权，导致随机冲突。

### 2.3 发现 I2C 触摸板初始化问题

```bash
ioreg -r -c VoodooI2CDeviceNub -w0
# 结果：
# TPD0 - VoodooI2CDeviceNub 识别了 (GPIOPin=35, i2cAddr=44, PNP0C50)
# 但 VoodooI2CHIDDevice = 0（VoodooI2CHID 未匹配）
```

**原因 1**：`Kernel/Add` 里 VoodooI2C 插件加载顺序错误  
```
[错误顺序]
[8] VoodooInput.kext (插件)
[9] VoodooI2CServices.kext (插件)
[10] VoodooGPIO.kext (插件)
[11] VoodooI2C.kext (主 kext) ← 主 kext 在插件之后！
[12] VoodooI2CHID.kext
```
VoodooI2C 主 kext 依赖这些插件，但插件在主 kext 之前加载，导致初始化时依赖关系混乱。

**原因 2**：VoodooPS2Controller 同时加载干扰了 VoodooI2C 的 I2C 总线初始化（PS2 和 I2C 共享 GPIO 中断资源）。

**结果**：TPD0 设备被 VoodooI2CDeviceNub 识别，但 VoodooI2CHID 未能匹配（`busy 3206ms` 说明初始化超时）。

---

## 三、修复方案

### 修改 1：禁用 VoodooPS2Controller（解决键盘冲突）

**原理**：
- 键盘由系统内置 `ApplePS2Controller → ApplePS2Keyboard` 驱动，已正常工作
- `VoodooPS2Controller` 是不必要的重复加载，且版本 2.3.5 在 macOS 15 上和系统 PS2 驱动存在竞争
- VoodooInput（触摸板手势）已内置在 VoodooI2C 2.9.x 的插件里，不再依赖 VoodooPS2

| 条目 | 修改前 | 修改后 |
|------|------|------|
| `VoodooPS2Controller.kext` | Enabled=True | **Enabled=False** |
| `VoodooPS2Controller.kext/Contents/PlugIns/VoodooPS2Keyboard.kext` | Enabled=True | **Enabled=False** |

### 修改 2：修正 VoodooI2C 加载顺序（解决触摸板不稳定）

**原理**：OpenCore 按 `Kernel/Add` 数组顺序注入 kext。主 kext 必须在其插件之前加载。

| # | 修改前（错误） | 修改后（正确） |
|---|------|------|
| 11 | VoodooInput.kext (插件) | **VoodooI2C.kext (主)** |
| 12 | VoodooI2CServices.kext (插件) | VoodooInput.kext (插件) |
| 13 | VoodooGPIO.kext (插件) | VoodooI2CServices.kext (插件) |
| 14 | VoodooI2C.kext (主) | VoodooGPIO.kext (插件) |
| 15 | VoodooI2CHID.kext | VoodooI2CHID.kext |

---

## 四、修改内容记录

### 4.1 备份

- 备份文件：`/tmp/config.plist.v8.0.bak`（重要！存到安全位置）
- 备份时间：2026-06-05 19:xx

### 4.2 修改后完整 Kernel/Add 顺序

```
[0]  Lilu.kext                              Enabled=True
[1]  VirtualSMC.kext                        Enabled=True
[2]  WhateverGreen.kext                     Enabled=True
[3]  SMCBatteryManager.kext                 Enabled=True
[4]  SMCDellSensors.kext                    Enabled=True
[5]  SMCProcessor.kext                      Enabled=True
[6]  AppleALC.kext                          Enabled=True
[7]  GenericCardReaderFriend.kext           Enabled=True
[8]  VoodooPS2Controller.kext               Enabled=False  ← 禁用（冲突）
[9]  VoodooPS2Keyboard.kext (PS2 插件)      Enabled=False  ← 禁用（冲突）
[10] itlwm.kext                             Enabled=True
[11] VoodooI2C.kext                         Enabled=True  ← 主 kext 移到前面
[12] VoodooI2C.kext/PlugIns/VoodooInput     Enabled=True
[13] VoodooI2C.kext/PlugIns/I2CServices     Enabled=True
[14] VoodooI2C.kext/PlugIns/VoodooGPIO      Enabled=True
[15] VoodooI2CHID.kext                      Enabled=True
```

---

## 五、预期效果

重启后：
- **键盘**：由 `ApplePS2Controller → ApplePS2Keyboard` 稳定驱动，不再有随机失效
- **触摸板**：VoodooI2C 正确初始化 → VoodooI2CHID 匹配 TPD0 → 触摸板稳定工作
- **VoodooI2CHIDDevice**：预期从 `=0` 变为 `=1`（用以下命令验证）

### 验证命令

```bash
# 触摸板驱动是否匹配（=1 表示成功）
ioreg -l -w0 | grep -o '"VoodooI2CHIDDevice"=[0-9]*'

# 键盘驱动（ApplePS2，稳定）
ioreg -p IOService -w0 | grep "ApplePS2Keyboard"

# I2C 触摸板设备（TPD0 下应有 VoodooI2CHID 子设备）
ioreg -r -c VoodooI2CHIDDevice -w0 | head -5
```

---

## 六、如果触摸板仍无效

进一步可能的原因：
1. **GPIO 中断未正确路由**：TPD0 用的是 GPIO 中断（gpioPin=35），需要 SSDT-GPI0 补丁
2. **VoodooI2C 2.9.1 对 macOS 15 支持不完整**：最新版本为 2.9.2+（从 GitHub 下载）
3. **ACPI 的 `_CRS` 方法未正确返回 GPIO 资源**：需要自定义 SSDT 补丁

如果需要进一步调试，可以运行：
```bash
log show --last 5m --predicate 'eventMessage CONTAINS "VoodooI2C"' 2>/dev/null | tail -30
```

---

## 七、回退方法

如果修改后键盘或触摸板反而变差，恢复方法：

**方法 1（推荐）**：从 Windows 挂载 EFI 盘，将备份文件覆盖回去：
```
cp /tmp/config.plist.v8.0.bak → EFI/OC/config.plist
```

**方法 2**：重新启用 VoodooPS2Controller（Enabled=True），并将 VoodooI2C 插件移回主 kext 之前（还原到 V7.8 配置）。
