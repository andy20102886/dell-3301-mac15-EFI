# V8.1 — CPU 电源管理修复（散热/风扇狂转）

> **日期**: 2025-06-05
> **问题**: Dell Latitude 3301 机身很烫，风扇一直全速狂转
> **根因**: 缺少 CPUFriend + CPUFriendDataProvider，macOS 无法正确管理 i5-8265U 频率调度

---

## 根因分析

```
没有 CPUFriend 时的状况：
  macOS 15 Sequoia
    → SSDT-PLUG 启用 XCPM（基础电源管理）✅
    → 但缺少 CPUFriend 频率策略 ❌
    → CPU 默认运行在较高频率/电压
    → 持续发热
    → EC 检测到高温 → 风扇狂转

添加 CPUFriend 后：
  macOS 15 Sequoia
    → SSDT-PLUG 启用 XCPM ✅
    → CPUFriend 注入频率策略 ✅
    → CPUFriendDataProvider 提供 Dell 3301 专属数据 ✅
    → CPU 可以降到 800MHz 空闲
    → EPP=0x80 平衡节能策略
    → 温度降低 → 风扇正常
```

---

## 修改内容

### 新增 Kext

| Kext | 来源 | 作用 |
|------|------|------|
| **CPUFriend.kext** | Acidanthera v1.2.9 | CPU 频率/电压动态调度 |
| **CPUFriendDataProvider.kext** | CPUFriendFriend 生成 | Dell 3301 i5-8265U 专属策略数据 |

### CPUFriendDataProvider 生成参数

| 参数 | 值 | 含义 |
|------|-----|------|
| Board | Mac-827FB448E656EC26 | MacBookPro15,2 |
| CPU | i5-8265U (Whiskey Lake) | 1.60GHz base |
| LFM | 0x08 | 最低频率 800MHz（默认 1200MHz 0x0C） |
| EPP | 0x80 | 平衡节能（默认 0x90 MacBookPro） |
| PerfBias | 0x05 | MacBook Pro 标准值 |
| 额外节能 | ✅ 启用 | 视频播放/Xcode/屏保等低功耗优化 |

### config.plist 变化

```
Kernel/Add 新增（插入在 SMCProcessor 之后）：
  [6] CPUFriend.kext
  [7] CPUFriendDataProvider.kext
```

---

## 加载顺序（关键！）

```
 1. Lilu.kext              ← 基础补丁引擎
 2. VirtualSMC.kext        ← SMC 模拟
 3. WhateverGreen.kext     ← GPU
 4. SMCBatteryManager.kext ← 电池
 5. SMCDellSensors.kext    ← Dell EC 传感器
 6. SMCProcessor.kext      ← CPU 温度传感器
 7. CPUFriend.kext         ← CPU 电源管理 ← 新增
 8. CPUFriendDataProvider.kext ← 频率策略数据 ← 新增（必须在 CPUFriend 之后）
 9. AppleALC.kext
10. GenericCardReaderFriend.kext
...
```

⚠️ **CPUFriendDataProvider 必须在 CPUFriend 之后加载**，否则数据注入无效。

---

## 还原方法

```bash
# 还原到修改前
cp /tmp/config.plist.v8.1-fan-backup.bak /Volumes/OC/EFI/OC/config.plist

# 删除新增的 kext
rm -rf /Volumes/OC/EFI/OC/Kexts/CPUFriend.kext
rm -rf /Volumes/OC/EFI/OC/Kexts/CPUFriendDataProvider.kext
```

---

## 验证方法

重启后执行：

```bash
# 1. 确认 CPUFriend 已加载
kextstat | grep CPUFriend

# 2. 查看 CPU 频率（应能看到 800MHz~3900MHz 动态范围）
sysctl -n machdep.xcpm.vector_loaded_count
# 期望输出: 1（说明频率向量已加载）

# 3. 查看 XCPM 状态
sysctl machdep.xcpm | head -10

# 4. 查看温度
sudo powermetrics --samplers smc -n 1 2>/dev/null | grep -E "temp|fan"

# 5. 监控实时频率/功耗
sudo powermetrics --samplers cpu_power -n 5 -i 1000 2>/dev/null | grep -A2 "CPU"
```
