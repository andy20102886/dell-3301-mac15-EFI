# V9.0 — GPU 修复（4MB VRAM + 背光暗）

> **日期**: 2025-06-07
> **机器**: Dell Latitude 3301（新机，带内屏）, macOS 15.5 Sequoia
> **问题**: 
>   1. 显存只有 4MB（无加速）
>   2. 内屏极暗（背光未正常工作）

---

## 根因分析

```
旧 config（V2 基线 + V8.1 CPUFriend）：
  ig-platform-id = 0x3EA50009（CFL 移动端 GT2，带 eDP）
  ↓
  macOS 15 已删除 CFL 图形驱动（仅剩 KBL 驱动）
  ↓
  CFL 帧缓冲区无法初始化
  ↓
  回退到 VESA 模式 → 4MB 显存
  ↓
  无 WhateverGreen 背光控制 → 屏幕极暗
```

**关键发现**：这台新机的 Lilu 1.6.9 + WEG 1.6.8 **正常加载了**（旧机不行）。
只需把 ig-platform-id 从 CFL 换成 KBL，WEG 就能正确初始化帧缓冲区。

---

## 修改内容

### GPU DeviceProperties 修改

| 属性 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `AAPL,ig-platform-id` | `0x3EA50009` (CFL) | **`0x59160000`** (KBL) | KBL 移动端 GT2，带 LVDS 内屏 |
| `device-id` | `0x5916` | `0x5916` | 不变，UHD 620 原生 |
| `framebuffer-stolenmem` | `0x3000` (3MB) | **`0x3001`** (19MB) | KBL 最低 19MB |
| `framebuffer-unifiedmem` | `0x06` (6) | **`0x80`** (128MB) | 旧值编码错误 |
| `framebuffer-conX-alldata` | 3个 CFL 端口数据 | **已删除** | CFL 端口布局不兼容 KBL |
| `framebuffer-conX-enable` | 3个 CFL 端口启用 | **已删除** | 让 WEG 自动管理连接器 |
| `framebuffer-patch-enable` | `1` | `1` | 不变 |
| `igfxonln` | `1` | `1` | 不变 |
| `model` | "Intel UHD Graphics 620" | 不变 | 不变 |

### 其他修改

| 项 | 旧值 | 新值 |
|----|------|------|
| boot-args | `debug=0x100 keepsyms=1` | `debug=0x100 keepsyms=1 -igfxblr` |
| csr-active-config | `00000000` (SIP 全开) | `FF070000` (SIP 全关) |
| SecureBootModel | Default | Disabled |

### `-igfxblr` 的作用

WhateverGreen `-igfxblr` 启动参数：绕过 macOS 原生背光控制，由 WEG 直接写入 PWM 寄存器。
**对于很多笔记本（包括 Dell），这是内屏背光正常工作的关键。**

---

## KBL ig-platform-id 为什么选 0x59160000

```
0x59160000（KBL 移动端 GT2）：
  连接器 0: LVDS ─── → 内屏 ← 这台机器有内屏！
  连接器 1: DP   ─── → HDMI（通过 WEG con-type 修正）
  连接器 2: DP   ─── → DP 输出
  连接器 3: DP   ─── → 预留
```

对比 0x3E9B0000（旧机用的，无 eDP/LVDS）——旧机没内屏所以需要这个。
新机有内屏，必须用带 LVDS 的 KBL 移动端 personality。

---

## 还原方法

```bash
cp /tmp/config.plist.v9.0-gpu-bak /Volumes/OC/EFI/OC/config.plist
```

---

## 验证方法

重启后执行：

```bash
# 1. 检查显存（预期 > 7MB，正常应该是 1536MB）
system_profiler SPDisplaysDataType | grep -A8 "Intel"

# 2. 检查 GPU kext 是否加载（预期看到 KBLGraphics 已加载）
kextstat | grep -i "KBL\|AppleIntel.*Graphics"

# 3. 检查 WhateverGreen 日志（预期无错误）
log show --last boot | grep -i "WhateverGreen\|igfx\|backlight"

# 4. 检查内屏背光是否正常（拉动亮度滑块是否有反应）
```

---

## 注意事项

⚠️ **如果重启后仍然 4MB/7MB 且屏幕暗**：
可能 KBL kext 被 macOS 15 kmutil 排除，需要 V7.8 方案（OC 直接注入 KBL kext）。
但先试这个方案——Lilu/WEG 已经正常加载了，KBL 帧缓冲大概率能初始化。
