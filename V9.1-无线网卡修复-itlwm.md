# V9.1 — 无线网卡修复（itlwm + HeliPort）

## 问题
- Wi-Fi 完全不可用，`ifconfig` 没有任何无线接口
- 系统设置 → 网络 中没有 Wi-Fi 选项

## 根因
1. **itlwm.kext 在 V9.0 修改中丢失**：OC/Kexts 目录中没有 itlwm.kext
2. **config.plist Kernel/Add 中没有 itlwm 注册条目**
3. V8.0 之前 itlwm 存在且正常工作，V9.0 用 Python plistlib dump→load→write 覆盖了 config.plist 时丢失

## 硬件
- 设备：**Intel Wi-Fi 6 AX201 (CNVi)**
- PCI ID：`pci8086,9df0`
- CNVi 集成在 PCH，不是独立 M.2 卡

## 修复内容

### 新增文件
- `/Volumes/OC/EFI/OC/Kexts/itlwm.kext`（v2.3.0 stable）
- `/Applications/HeliPort.app`（v2.0.0-alpha）

### config.plist 修改
```
Kernel/Add 索引 3（在 Lilu 之后，WhateverGreen 之前）:
  BundlePath: itlwm.kext
  Enabled: True
  MinKernel: 23.0.0  ← 限制只加载在 macOS 14+
  ExecutablePath: Contents/MacOS/itlwm
  Comment: Intel Wi-Fi 6 AX201 (CNVi) - requires HeliPort app
```

### 为什么不用 AirportItlwm？
- AirportItlwm 需要对应 macOS 版本，最新只到 Sonoma 14.4
- macOS 15 Sequoia 没有对应版本
- **itlwm.kext 通用版** 不依赖 macOS 版本，通过 HeliPort.app 管理连接

## HeliPort 使用说明
1. 重启后，itlwm 会自动加载
2. 打开 `/Applications/HeliPort.app`
3. 菜单栏出现 Wi-Fi 图标，点击扫描并连接网络
4. 建议设置 HeliPort 开机自启：系统设置 → 通用 → 登录项 → 添加 HeliPort

## 备份
- `/tmp/config.plist.v9.1-wifi-bak`

## 还原
```bash
cp /tmp/config.plist.v9.1-wifi-bak /Volumes/OC/EFI/OC/config.plist
rm -rf /Volumes/OC/EFI/OC/Kexts/itlwm.kext
```
