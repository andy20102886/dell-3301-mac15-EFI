# V9.9 → V9.10 — 背光修复：WEG 自适应

## 进度

- V9.6: 关 smoother → 闪烁未解决
- V9.7: `-igfxblt`→`-igfxblr` → 不闪但太暗
- V9.8: 加 `applbkl=0` → 滑块消失 ❌
- V9.9: 替换通用 PNLF + `-igfxblr` → 滑块回来但最暗不可调 ⚠️

## V9.10 修复

```
boot-args: 移除 -igfxblr，让 WEG 完全自适应
PNLF:      Dortania 通用版（自动检测平台）
```

推理：`-igfxblr` 强制 KBL 寄存器方法，但 Dell 3301 的 PWM 寄存器
可能不完全匹配标准 KBL 布局。移除所有背光 boot-arg 后，
WEG + 通用 PNLF 会根据硬件自适应选择正确方法。

## 备份

- V9.9: `/Volumes/OC/config-backup-v9.9.plist`
- V9.10: `/Volumes/OC/config-backup-v9.10.plist`（相当于 V9.9 但无 -igfxblr）

## Plan B（如果仍然暗）

```bash
# 改回 -igfxblt（之前亮，现在有通用 PNLF 应该不闪）
```
