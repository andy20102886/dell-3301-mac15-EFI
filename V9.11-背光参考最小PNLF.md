# V9.11 — 背光修复：参考成功案例最小 PNLF

## 失败历史

```
V9.6: 关 smoother              → 闪烁未解决
V9.7: -igfxblr                 → 不闪但太暗
V9.8: +applbkl=0               → 滑块消失 ❌
V9.9: 通用 PNLF + -igfxblr     → 滑块回来但最暗
V9.10: 移除 backlight flag     → 最暗不可调
```

## V9.11 修复

**完全照搬 sambow23 参考案例的背光方案**：

### 对比

| 项目 | 旧（V9.9/V9.10） | 新（V9.11） |
|------|-----------------|------------|
| PNLF | Dortania 通用 1110 bytes | **参考同款 105 bytes** |
| PNLF 内容 | CFL/KBL PWMMAX/LMAX 等 | **仅 _HID + _CID + _UID** |
| boot-args | 无背光 flag | **-igfxblr** |

### 关键差异

旧 PNLF（1110 bytes）= 通用版，内含 Sandy/Ivy/Haswell/CFL 多套 PWMMAX 参数，
通过 `_OSI("Darwin")` 分支自动选择。但某套参数和 Dell 3301 的屏幕 PWM 不匹配。

新 PNLF（105 bytes）= 最小版，只注册 `APP0002` 设备，**不预设任何 PWM 参数**。
WEG 检测到 PNLF 存在后，完全根据 `-igfxblr` + 硬件自动选择正确的 PWM 值。

来源：sambow23/Dell-Latitude-3301-macOS（同机型，已验证通过）

## 备份

- V9.10: `/Volumes/OC/config-backup-v9.10.plist`
- V9.11: `/Volumes/OC/config-backup-v9.11.plist`

## 还原

```bash
cp /Volumes/OC/config-backup-v9.10.plist /Volumes/OC/EFI/OC/config.plist
```
