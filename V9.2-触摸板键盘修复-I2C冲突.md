# V9.2 — 触摸板/键盘修复（I2C 冲突）

## 问题

- 触摸板和键盘在开机后前几秒可以正常使用，之后就完全失效
- `ioreg` 显示 `AppleIntelLpssI2C (3.0.60)` 已加载，抢占了 I2C 总线
- `VoodooI2CHID` 已加载但 `0 links`（无法连接到设备）
- `VoodooPS2Controller` 与系统原生 `ApplePS2Controller` 同时加载造成键盘冲突

## 根因分析

```
┌─────────────────────────────────────────────────────────┐
│ AppleIntelLpssI2C.kext  ← 原生 I2C 驱动（抢先加载）      │
│   ↓ 占用 I2C 总线                                         │
│ VoodooI2C.kext 无法获取 TPD0（触摸板）连接                │
│                                                          │
│ VoodooPS2Controller.kext  ← 第三方 PS2 驱动              │
│   + ApplePS2Controller.kext  ← 系统原生 PS2 驱动          │
│   → 两个驱动抢同一个 PS2 键盘 → 随机冲突 → 键盘失效       │
└─────────────────────────────────────────────────────────┘
```

## 修复内容

### 1. 添加 Kernel/Block 阻止 Apple 原生 I2C 驱动

```xml
<!-- Kernel/Block 新增 3 个条目：-->
1. com.apple.driver.AppleIntelLpssI2C           → Exclude
2. com.apple.driver.AppleIntelLpssI2CController  → Exclude
3. com.apple.driver.AppleIntelLpssDmac            → Exclude
```

### 2. 禁用 VoodooPS2Controller（键盘用系统原生驱动）

```
VoodooPS2Controller.kext → Enabled=False
VoodooPS2Keyboard.kext   → Enabled=False
```

### 3. 保持 VoodooI2C 加载顺序正确

```
VoodooI2C.kext      ← 主 kext 先加载
VoodooI2CHID.kext   ← HID 插件后加载
```

## 备份文件

- `/tmp/config.plist.v9.2-input-bak` — 修复前版本
- 也复制了一份到 OC 盘：`/Volumes/OC/config-backup-v9.1.plist`

## 还原方法

```bash
# macOS Recovery / UEFI Shell:
cp /Volumes/OC/config-backup-v9.1.plist /Volumes/OC/EFI/OC/config.plist

# 或在 Windows 里：
# 打开 OC 盘 → EFI/OC/config.plist
# 用记事本编辑，找到 <key>Block</key> 区段
# 删除新增的 I2C Block 条目（或把 <true/> 改为 <false/>）
```

## 验证方法

重启进入 macOS 后：
```bash
# 确认 Apple I2C 驱动已被阻止
kextstat | grep -i "AppleIntelLpssI2C"  # 应该无输出

# 确认 VoodooI2C 正常加载
kextstat | grep -i "VoodooI2C"           # 应有 VoodooI2C + VoodooI2CHID

# 确认触摸板可用
# → 直接操作触摸板，看是否持续正常工作
```
