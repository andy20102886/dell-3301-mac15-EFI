# V9.3 — 触摸板/键盘修复（参考成功案例）

## 问题

- 触摸板和键盘完全无反应（V9.2 修复后）
- V9.2 以前：开机后前几秒可以用，之后失效

## 根因分析（参考 sambow23/Dell-Latitude-3301-macOS）

原方案（V9.2）错在：
1. **VoodooPS2Controller 被禁用** — 键盘是 PS2 协议，必须靠它
2. **VoodooPS2Controller 插件不全** — 只有 Keyboard，缺少 Mouse/Trackpad/VoodooInput
3. **缺少 SSDT-XOSI.aml** — `_OSI` 重命名基础，让 ACPI 以 Darwin 模式暴露 GPIO
4. **缺少 SSDT-TPD0-GPIO.aml** — TPD0 触摸板的 GPIO 中断引脚补丁
5. **Kernel/Block 阻止了 Apple I2C** — 参考配置不阻止，VoodooI2C 与 Apple 驱动可以共存
6. **VoodooI2C 插件顺序错误** — GPIO 插件必须在主 kext 之前加载

## 修复内容（照搬成功案例）

### 1. 输入设备 kext（完全替换）

| kext | 旧版本 | 新版本 | 变化 |
|------|---------|--------|------|
| VoodooPS2Controller | 2.3.5（仅 Keyboard） | **2.3.7（完整版）** | + Mouse + Trackpad + VoodooInput |
| VoodooI2C | 2.9.1 | 2.9.1 | 插件顺序修正 |
| VoodooI2CHID | 1.0 | 1.0 | 无变化 |
| **BrightnessKeys** | ❌ 缺失 | **✅ 新增** | 亮度快捷键 |

### 2. ACPI 补丁

```
ACPI/Add 新增：
  [0]  SSDT-XOSI.aml   ← 必须第一个加载
  ...
  [11] SSDT-TPD0-GPIO.aml  ← TPD0 GPIO 中断补丁

ACPI/Patch 新增：
  [✅] _OSI → XOSI rename（Darwin 兼容）
  [✅] _STA → XSTA rename（IRQ 冲突修复）
```

### 3. Kernel/Add 加载顺序（关键！）

```
 [4]  VoodooPS2Controller.kext          ← 主 kext
 [5]  VoodooPS2.../VoodooPS2Keyboard.kext
 [6]  VoodooPS2.../VoodooPS2Mouse.kext
 [7]  VoodooPS2.../VoodooPS2Trackpad.kext
 [8]  VoodooPS2.../VoodooInput.kext  ← Disabled（参考也是）
 [9]  VoodooI2C.../VoodooI2CServices.kext  ← 插件先加载
 [10] VoodooI2C.../VoodooGPIO.kext           ← GPIO 引脚补丁
 [11] VoodooI2C.../VoodooInput.kext
 [12] VoodooI2C.kext                      ← 主 kext 在插件之后！
 [13] VoodooI2CHID.kext
 [14] BrightnessKeys.kext                ← 亮度快捷键
```

### 4. Kernel/Block 清空

```
❌ 不再阻止 AppleIntelLpssI2C（参考配置不阻止）
```

## 参考来源

- GitHub: [sambow23/Dell-Latitude-3301-macOS](https://github.com/sambow23/Dell-Latitude-3301-macOS)
- 配置：OpenCore 1.0.4 + macOS Ventura 13.7.6
- 说明：该 EFI 使用 **DSDT.aml 完整替换** 来实现触摸板
  （本修复改用 SSDT-TPD0-GPIO.aml 热补丁，更安全）

## 备份文件

- `/Volumes/OC/config-backup-v9.2.plist` — V9.2 修复前
- `/Volumes/OC/config-backup-v9.3.plist` — V9.3 修复前（本次新增）

## 还原方法

```bash
# macOS Recovery / Windows 里：
cp /Volumes/OC/config-backup-v9.2.plist /Volumes/OC/EFI/OC/config.plist
```

## 待验证

重启后测试：
1. 键盘是否能正常输入（内置键盘）
2. 触摸板是否能移动光标、点击
3. 亮度快捷键（F11/F12 或对应键）是否有效
