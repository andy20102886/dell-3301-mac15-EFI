# V9.4 — 触摸板/键盘修复：卡苹果图标诊断

## 问题

V9.3 配置开机卡死在苹果图标，无进度条。

## 根因：全局 _STA → XSTA 重命名破坏 ACPI

### 出问题的补丁

| 参数 | 值 | 含义 |
|------|-----|------|
| Comment | Rename _STA to XSTA (broad) | |
| Count | 0 | 无限次 |
| Limit | 0 | 不限制 |
| Skip | 0 | 不跳过 |
| OemTableId | (空) | **所有表** |
| TableSignature | (空) | **所有类型** |
| TableLength | 0 | **所有长度** |

这个补丁重命名了**所有 ACPI 表中每一个 _STA 方法**。

### 为什么致命

_STA（Status）是 ACPI 最底层的方法，操作系统用它判断：
- 设备是否存在（bit 0）
- 设备是否启用（bit 1）
- 设备是否正常工作（bit 2-3）

全局重命名后，没有设备能正确报告状态。ACPI 设备枚举机制完全崩溃 → 内核无法初始化硬件 → 开机卡死。

### 为什么 sambow23 参考有 _STA 改名但能启动

sambow23 参考的 _STA → XSTA 是 **HPET 专用**的（Comment 写 "HPET _STA to XSTA"），作用域限于 HPET 设备表。V9.3 把它误用成了全局。

### SSDT 验证

| SSDT | 大小 | 引用 XSTA？ | 需要 _STA 改名？ |
|------|------|------------|-----------------|
| SSDT-TPD0.aml | 103B | 否（只引用 _OSI.Darwin） | 不需要 |
| SSDT-TPD0-GPIO.aml | 133B | 否（只写 SBFG GPIO） | 不需要 |

**结论：两个 SSDT 都不需要 _STA → XSTA 改名，改名纯属多余且致命。**

## V9.4 修复

### 改动

只改一处：_STA → XSTA 补丁 Enabled: true → false

### 保留不变

| 组件 | 版本 | 说明 |
|------|------|------|
| SSDT-XOSI.aml | — | Darwin 兼容 |
| SSDT-TPD0.aml | — | TPD0 设备定义 |
| SSDT-TPD0-GPIO.aml | — | GPIO 中断引脚补丁 |
| VoodooPS2Controller | 2.3.7 | +Keyboard+Mouse+Trackpad |
| VoodooI2C | 2.9.1 | +Services+GPIO+Input |
| VoodooI2CHID | — | HID 设备支持 |
| BrightnessKeys | — | 亮度快捷键 |
| VoodooInput (PS2) | Disabled | 避免与 I2C 冲突 |
| Kernel/Block | (空) | 不阻止任何 kext |

### 加载顺序（关键）

I2C 体系：插件先于主 kext 加载

### 参考

- [sambow23/Dell-Latitude-3301-macOS](https://github.com/sambow23/Dell-Latitude-3301-macOS)
- V9.3 文档：D:\V9.3-触摸板键盘修复-参考成功案例.md

## 测试验证

重启后检查：
1. 键盘是否能正常输入
2. 触摸板是否能移动光标、点击
3. 亮度快捷键是否有效

## 如果仍不正常

1. 检查 SSDT-TPD0-GPIO.aml GPIO pin 是否匹配 3301
2. 尝试移除 SSDT-TPD0-GPIO.aml（仅用 SSDT-TPD0.aml）
3. Kernel/Block 添加 AppleIntelLpssI2C
4. 升级 VoodooI2C 到 2.10+
