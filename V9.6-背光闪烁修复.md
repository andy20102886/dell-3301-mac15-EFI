# V9.6 — 背光闪烁修复

## 问题

- 系统设置里调整亮度：只有最亮正常
- 调到其他亮度：屏幕每秒闪烁一次

## 根因

AppleBacklightSmoother 在 Hackintosh 上导致频闪。

GPU DeviceProperties 中有 4 个背光平滑器参数：
```
enable-backlight-smoother: 01000000   ← 启用
backlight-smoother-interval: 07000000
backlight-smoother-steps: 23000000
backlight-smoother-threshold: 2c010000
```

AppleBacklightSmoother 功能是在亮度切换时做渐进过渡。
但在 Hackintosh 上，平滑器使用的 PWM 频率/寄存器地址与硬件不匹配，
导致每 1 秒向错误的 PWM 寄存器写入一次 → 屏幕闪烁。

这是 WhateverGreen 的已知问题。

## 修复

禁用背光平滑器 + 删除所有 smoother 参数：

```
Before:
  enable-backlight-smoother: 01000000 → After: 00000000 (禁用)
  backlight-smoother-interval: 删除
  backlight-smoother-steps: 删除
  backlight-smoother-threshold: 删除
```

## 备份

- `/Volumes/OC/config-backup-v9.6.plist`

## 还原

```bash
cp /Volumes/OC/config-backup-v9.6.plist /Volumes/OC/EFI/OC/config.plist
```
