# V9.7 — 背光闪烁修复

## 问题

- 系统设置里调整亮度：只有最亮正常
- 调到其他亮度：屏幕每秒闪烁一次

## 根因（V9.6 失败后的深入诊断）

**双重错误叠加**：

1. **SSDT-PNLF.aml 是 CFL 版**（1122 bytes，含 CFLMAX/CFLEVW/CFGRAN/CFFBTP）
   — 但 GPU 是 KBL，PWM 寄存器地址不匹配

2. **boot-arg `-igfxblt`**（Backlight Transistor，CFL/ICL 方法）
   — 但硬件是 KBL，应该用 `-igfxblr`（Backlight Register）

两者叠加 → WhateverGreen 往错误的 PWM 寄存器写值 → 每秒一次 → 闪烁。

## V9.6 失败
- 只禁用 AppleBacklightSmoother → 无效，仍然闪烁

## V9.7 修复

```
boot-args: -igfxblt → -igfxblr
  ↓
BLT (Transistor) = CFL/ICL 背光方法 ❌
BLR (Register)   = KBL 背光方法       ✅
```

## 备份

- V9.6: `/Volumes/OC/config-backup-v9.6.plist`
- V9.7: `/Volumes/OC/config-backup-v9.7.plist`

## 还原

```bash
cp /Volumes/OC/config-backup-v9.7.plist /Volumes/OC/EFI/OC/config.plist
```
