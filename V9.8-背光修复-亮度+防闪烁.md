# V9.8 — 背光终极修复：亮度 + 防闪烁

## 问题

- V9.7 改 `-igfxblr` 后：不闪烁了，但屏幕太暗
- V9.5 之前用 `-igfxblt`：屏幕很亮，但每秒闪烁

## 根因

历史梳理：

```
V9.0: -igfxblr → 亮度 ✅ 正常
       (但当时 V9.0 同时删了 framebuffer-conX-alldata，后来被恢复了)

中间: -igfxblt → 亮度 ✅ 但闪烁 ❌
       (CFL 背光方法往错误寄存器写值)

V9.7: -igfxblr → 不闪 ✅ 但太暗 ❌
       (KBL 正确方法，但 CFL 版 PNLF 限制了最大亮度)
```

**真正根因**：SSDT-PNLF.aml 是 CFL 版（含 CFLMAX/CFLEVW 等 CFL 参数），
这些参数设置了错误的 PWM 最大值。WEG 用 `-igfxblr` 读写 KBL 寄存器时，
受 PNLF 里 CFL 的 LMAX 限制 → 亮度上不去。

## V9.8 修复

```
boot-args: -igfxblr applbkl=0
              ↑         ↑
              │         └─ 禁用 AppleBacklight，WEG 全权控制
              └─ KBL 背光寄存器方法（不闪烁）
```

`applbkl=0`：告诉 WhateverGreen 接管所有背光控制，
不再读取 SSDT-PNLF 里的 CFL 参数，自动检测正确 PWM 值。

## 备份

- V9.7: `/Volumes/OC/config-backup-v9.7.plist`
- V9.8: `/Volumes/OC/config-backup-v9.8.plist`

## 如果仍然太暗（Plan B）

需要替换 SSDT-PNLF.aml 为 KBL 专用版本：
```bash
# V9.9 会用到的方案，已准备好
```
