# V9.8 → V9.9 — 背光终极修复

## 问题历史

- V9.6: 关 smoother → 不闪但暗，滑块正常
- V9.7: `-igfxblt` → `-igfxblr` → 不闪但太暗
- V9.8: 加 `applbkl=0` → **亮度滑块消失，屏幕最暗** ❌

## V9.8 失败根因

`applbkl=0` 完全禁用了 AppleBacklight.kext，导致系统不认背光设备 → 滑块消失。

## V9.9 修复（正确方案）

### 修改 1：回退 boot-args
```
已回退: applbkl=0 ← 删除（太激进）
保留:   -igfxblr   ← KBL 背光方法（不闪烁）
```

### 修改 2：替换 SSDT-PNLF.aml（核心）
```
旧: CFL 专用版 (1122 bytes) → CFLMAX/CFLEVW/CFGRAN/CFFBTP
新: Dortania 通用版 (1110 bytes) → 自动检测平台，使用正确 PWM 值
```

来源: https://github.com/dortania/Getting-Started-With-ACPI

通用版 PNLF 会根据 `_OSI("Darwin")` 自动选择 KBL 对应的 PWMMAX 值，
而不是强制使用 CFL 的 `0xFFFF`。

### 当前 boot-args
```
debug=0x100 keepsyms=1 -igfxblr
```

### 当前 SSDT-PNLF
```
1110 bytes (Dortania 通用版)
备份: SSDT-PNLF-CFL.bak.aml
```

## 备份

| 文件 | 说明 |
|------|------|
| `/Volumes/OC/config-backup-v9.8.plist` | V9.8 配置 |
| `/Volumes/OC/config-backup-v9.7.plist` | V9.7 配置 |
| `/Volumes/OC/EFI/OC/ACPI/SSDT-PNLF-CFL.bak.aml` | 旧 CFL PNLF |

## 还原

```bash
# 还原 config
cp /Volumes/OC/config-backup-v9.8.plist /Volumes/OC/EFI/OC/config.plist

# 还原 PNLF
cp /Volumes/OC/EFI/OC/ACPI/SSDT-PNLF-CFL.bak.aml /Volumes/OC/EFI/OC/ACPI/SSDT-PNLF.aml
```
