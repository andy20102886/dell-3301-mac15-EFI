# Dell Latitude 3301 黑苹果修复文档索引

> **重要**：此文档位于 OC 盘根目录，在 Windows 里可以直接访问。
> 一旦 macOS 启动失败，进 Windows 后打开此文件查看还原方法。

---

## 📋 版本修复记录（按时间顺序）

| 版本 | 修复内容 | 文档名称 | 状态 |
|------|---------|---------|------|
| V1~V7.3 | GPU 7MB VRAM 试错（失败） | `GPU驱动完整试错记录-V1toV7.8.md` | ❌ 未成功 |
| V7.4 | Lilu 强制加载（-liluforce） | `V7.4-Lilu强制加载修复文档.md` | ❌ 无效 |
| V7.5 | 重新下载 Lilu + WEG 替换 | `V7.5-全新下载kext替换文档.md` | ❌ 无效 |
| V7.6 | OCLP Root Patch 启用 | `V7.6-OCLP启用RootPatch文档.md` | ❌ OCLP 灰色 |
| V7.7 | OCLP 命令行强制补丁 | `V7.7-OCLP命令行强制RootPatch.md` | ❌ 不适用 |
| V7.8 | OpenCore 强制注入 KBL kext | `V7.8-OpenCore强制注入KBL驱动.md` | ⏳ 待测 |
| V8.0 | 触摸板/键盘修复（VoodooI2C 顺序） | `V8.0-触摸板键盘修复.md` | ⏳ 待测 |
| V8.1 | CPU 电源管理（散热风扇狂转） | `V8.1-CPU电源管理修复-散热风扇.md` | ✅ 已验证 |
| V9.0 | GPU 4MB VRAM + 屏幕太暗 | `V9.0-GPU修复-4MB+背光.md` | ✅ 已验证 |
| V9.1 | 无线网卡失效（itlwm 丢失） | `V9.1-无线网卡修复-itlwm.md` | ✅ 已验证 |
| V9.2 | 触摸板/键盘修复（I2C冲突） | `V9.2-触摸板键盘修复-I2C冲突.md` | ❌ 无效 |
| V9.3 | 照搬 sambow23 成功案例 | `V9.3-触摸板键盘修复-参考成功案例.md` | ❌ 卡苹果 |
| V9.5 | 触摸板/键盘修复（安全版） | `V9.5-触摸板键盘修复-安全版.md` | ✅ 已验证 |
| V9.6 | 亮度闪烁（关 smoother） | `V9.6-背光闪烁修复.md` | ❌ 无效 |
| V9.7 | 亮度闪烁（-igfxblr） | `V9.7-背光闪烁修复.md` | ⚠️ 不闪但太暗 |
| V9.8 | 亮度+防闪烁（applbkl=0） | `V9.8-背光修复-亮度+防闪烁.md` | ❌ 滑块消失 |
| V9.9 | 替换通用 PNLF + -igfxblr | `V9.9-背光终极修复.md` | ⏳ 待测 |

---

## 🆘 启动失败还原方法

### 方法 1：按版本逐步还原

```bash
# 特别注意：/tmp 目录里的备份重启后会丢失！
# 从 V9.2 开始，所有备份都直接放在 OC 盘根目录下

# 还原到 V9.1（config-backup-v9.1.plist）
cp /Volumes/OC/config-backup-v9.1.plist /Volumes/OC/EFI/OC/config.plist

# 还原到 V9.2 修复前（config-backup-v9.2.plist）
cp /Volumes/OC/config-backup-v9.2.plist /Volumes/OC/EFI/OC/config.plist

# 还原到 V9.5 修复前（config-backup-v9.5.plist）
cp /Volumes/OC/config-backup-v9.5.plist /Volumes/OC/EFI/OC/config.plist
```

**注意**：V9.0 和 V9.1 的备份曾经放在 `/tmp/`，在重启后已丢失。如果 V9.2 失败，只能还原到 V9.2 修复前的状态。

### 方法 2：在 Windows 里还原

1. 重启进 Windows
2. 打开 `此电脑` → 找到 OC 盘（通常是 `EFI (D:)` 或类似）
3. 打开 `EFI/OC/config.plist`（用记事本或 Notepad++）
4. 手动修改：
   - 找到 `<key>Enabled</key>` 后面跟着 `<true/>` 的 kext
   - 把新加的 kext（CPUFriend、itlwm 等）改回 `<false/>`
5. 保存后重启

### 方法 3：用备份文件还原（最简单）

OC 盘的 `/tmp/` 目录里可能有备份，但 **macOS 的 /tmp 是内存盘，重启会丢失**。

**正确做法**：备份应该放在 OC 盘根目录，而不是 /tmp：

```bash
# 在 macOS 里执行一次，把备份复制到 OC 盘根目录：
cp /tmp/config.plist.v9.0-gpu-bak /Volumes/OC/config-backup-v9.0.plist
cp /tmp/config.plist.v9.1-wifi-bak /Volumes/OC/config-backup-v9.1.plist
cp /tmp/config.plist.v9.2-input-bak /Volumes/OC/config-backup-v9.2.plist
```

---

## ✅ 当前已验证的正常功能

- ✅ GPU 加速（1536 MB VRAM，Metal 3）
- ✅ 无线网卡（itlwm + HeliPort）
- ✅ CPU 电源管理（CPUFriend，最低 800MHz）
- ✅ 电池管理（SMCBatteryManager）
- ✅ 传感器（SMCDellSensors，风扇/温度）
- ✅ 内置键盘（VoodooPS2Controller + VoodooPS2Keyboard 2.3.7）
- ✅ 触摸板（VoodooI2C 2.9.1 + VoodooI2CHID + GPIO 引脚补丁）

## ⚠️ 已知待修复

- ⚠️ CPU 温度偏高（WorkBuddy/Electron 自身占用 ~192%，闲时可接受）
- ⚠️ 风扇转速无法通过 SMC 直接读取（SMCDellSensors 在 macOS 15 上传感器键值异常）

---

## 📁 所有文档列表

```
OC 盘根目录/
├── 修复文档索引.md                    ← 本文件
├── README.md                          ← OC 盘说明
├── GPU驱动完整试错记录-V1toV7.8.md   ← V1~V7.8 完整记录
├── GPU驱动修复试错总结.md             ← GPU 修复总结
├── V6-KBL驱动修复技术文档.md         ← V6 技术文档
├── V7-完整修复记录.md                ← V7 记录
├── V7.4-Lilu强制加载修复文档.md
├── V7.5-全新下载kext替换文档.md
├── V7.6-OCLP启用RootPatch文档.md
├── V7.7-OCLP命令行强制RootPatch.md
├── V7.8-OpenCore强制注入KBL驱动.md
├── V8.0-触摸板键盘修复.md
├── V8.1-CPU电源管理修复-散热风扇.md
├── V9.0-GPU修复-4MB+背光.md
├── V9.1-无线网卡修复-itlwm.md
├── V9.3-触摸板键盘修复-参考成功案例.md
├── V9.5-触摸板键盘修复-安全版.md   ← 最新 ✅
```

---

## 📞 紧急联系

如果所有方法都失败了，最后的手段：

1. **重装 OC**：用 [OpenCore Install Guide](https://dortania.github.io/OpenCore-Install-Guide/) 重新制作
2. **换回 Windows**：在 BIOS 里把 Boot Order 改回 Windows Boot Manager 第一
3. **清除 NVRAM**：开机按 `F12` → `Reset NVRAM`

---

**最后更新**：2026-06-07 22:49
**当前版本**：V9.5（触摸板+键盘 ✅）
